{
  "name": "agba-brain",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server.js",
    "build": "rm -rf public && mkdir -p public && cp -R web/. public/ && echo 'Static web application ready'",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
